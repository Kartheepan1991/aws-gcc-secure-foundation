# Ingress disabled for initial deployment
# AWS Load Balancer Controller required for ALB ingress
# Use: kubectl port-forward svc/gcc-app-service 8080:80
# Or install nginx-ingress for basic ingress functionality

# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: gcc-app-ingress
#   annotations:
#     kubernetes.io/ingress.class: nginx
# spec:
#   rules:
#   - http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: gcc-app-service
#             port:
#               number: 80
