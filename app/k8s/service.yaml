apiVersion: v1
kind: Service
metadata:
  name: gcc-sample-service
  labels:
    app: gcc-sample-service
spec:
  type: NodePort
  selector:
    app: gcc-sample-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    name: http
